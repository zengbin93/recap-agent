# 复盘工程环境变量样例（复制为 .env 并填入真实值；.env 已被 .gitignore 忽略）

# ===== 必需 =====

# 1) Tushare 数据采集认证
#    申请：https://tushare.pro 注册 → 个人主页 → API Token
TUSHARE_TOKEN=

# 2) 飞书推送目标（单个 JSON，daily 必填；weekly/monthly 缺省时回退到 daily）
#    在飞书群「设置 → 群机器人 → 添加自定义机器人」拿到 webhook，key 取 URL 末段。
#    最简（三任务都发同一群）：
#      FEISHU_WEBHOOKS={"daily":{"key":"xxxxxxxx-xxxx-xxxx"}}
#    分群 + 启用签名（按需）：
#      FEISHU_WEBHOOKS={"daily":{"key":"d-key","sign_secret":"s1"},"weekly":{"key":"w-key"}}
FEISHU_WEBHOOKS=

# 3) Anthropic API Key（GitHub Actions 跑 Claude Code 必需；本地 dry run 不需要）
#    https://console.anthropic.com/
ANTHROPIC_API_KEY=
